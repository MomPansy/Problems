def checkio(array: list):
    numbers_to_sum = []
    if len(array) == 0:
        return 0
    for num in array:
        if array.index(num) % 2 == 0:
            numbers_to_sum.append(num)
    return numbers_to_sum
