def end_zeros(num):
    numbers = list(str(num))
    zeros = 0
    for i in range(len(numbers)):
        #print(numbers[-i-1])
        if numbers[-i-1] == '0':
            zeros += 1
        if numbers[-i-1] != '0':
            return zeros
    return zeros


print(end_zeros(0))
